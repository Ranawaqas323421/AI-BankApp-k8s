kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # NodePort jo bankapp service use karegi
        hostPort: 8080         # EC2 ka port 8080 se access hoga
        protocol: TCP
